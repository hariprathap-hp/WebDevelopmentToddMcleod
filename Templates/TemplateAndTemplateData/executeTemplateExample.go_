package main

import (
	"log"
	"os"
	"text/template"
)

var tmpl *template.Template

/*func init() {
	tmpl = template.Must(template.ParseFiles("tpl1.gohtml"))
}*/

func main() {
	err := tmpl.ExecuteTemplate(os.Stdout, "tpl1.gohtml", 42)
	if err != nil {
		log.Fatalln(err)
	}
}
