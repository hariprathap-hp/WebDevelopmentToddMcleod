<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>

<body>
    {{range .}}
        {{.AcaYear}}
        {{if eq .Fall.Term "Spring"}}
        {{range .Fall.Courses}}
            {{.Name}}
        {{end}}
        {{end}}
    {{end}}
</body>

</html>